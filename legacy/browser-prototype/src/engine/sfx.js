// SFX hook stubs — no audio yet. Fill in play() when the audio pass ships.
// Exports: sfx

const sfx = {
  play(name) {
    // Future audio pass: look up 'name' in a sound registry and play it.
    void name;
  },
};

export default sfx;
